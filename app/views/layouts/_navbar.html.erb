<body data-spy="scroll" data-offset="50" data-target=".navbar-collapse">
  <div class="navbar navbar-fixed-top navbar-default" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon icon-bar"></span>
          <span class="icon icon-bar"></span>
          <span class="icon icon-bar"></span>
        </button>
        <a href="#" class="navbar-brand"><img src='<%= asset_path 'logo.png' %>' class="img-responsive" alt="logo"></a>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <li> <%= link_to "HOME", root_path, class:'smoothScroll' %> </li>
          <li><%= link_to "Indice de Usuarios", professional_index_path , class:'smoothScroll' %> </li>
          <li><%= link_to "Tus Citas" ,user_appointments_path(current_user), class:'smoothScroll' %></li>
          <% else %>
          <li> <%= link_to "HOME", root_path, class:'smoothScroll' %> </li>
          <% end %>
          <% if user_signed_in? %>
              <li> <%= link_to "Salir", destroy_user_session_path, method: :delete, class:'smoothScroll' %> </li>
            <% else %>
            <li>  <%= link_to "Ingresar", new_user_session_path, class:'smoothScroll' %> </li>
            <li>  <%= link_to "Registrar", new_user_registration_path, class:'smoothScroll' %> </li>
            <% end %>
            <li><%= link_to "QUIENES SOMOS", about_about_us_path, class: 'smoothScroll'%></li>
        </ul>
      </div>
    </div>
  </div>
